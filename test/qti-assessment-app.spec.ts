import { test, expect, describe, it } from 'vitest';
import { QtiAssessmentApp } from '../src/qti-assessment-app';

describe('Assessment app', () => {
  it('should be defined', () => {
    expect(QtiAssessmentApp).toBeDefined();
  });
});
